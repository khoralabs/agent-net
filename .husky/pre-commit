#!/usr/bin/env sh
set -e

cd "$(git rev-parse --show-toplevel)"

echo "pre-commit: Biome format & lint (run 'bun run format' to fix)…"
bun run format:check

echo "pre-commit: Typecheck…"
bun run typecheck
